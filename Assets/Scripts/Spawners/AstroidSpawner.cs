using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Scuffed Class to test Astroid Spawning
/// </summary>
public class AstroidSpawner : MonoBehaviour
{
    //[SerializeField] private float spawnInterval = 2f;
    //[SerializeField] private Transform playerLocation;
    //private float spawnIntervalTime = 0;
    //private void Update()
    //{ 
    //    if (Time.time >= spawnIntervalTime)
    //    {
    //        float randomPoint = Random.RandomRange();
    //        Pooler.Instance.SpawnProjectile(PoolerType.Astroid, )
    //        spawnIntervalTime = Time.time + spawnInterval;
    //    }
    //}
}
